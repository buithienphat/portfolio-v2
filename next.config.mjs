/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "vercel.com", "raw.githubusercontent.com"], // Replace with the actual domains
  },
};

export default nextConfig;
